# 黄金矿工 SWF 初步分析

## 检查结果

- 原文件：`C:/Users/lim/Downloads/黄金矿工.swf`，保持原文件不变。
- SHA-256：`0c3d410285d69a36022f652b0c28e5c5f6d56b6112d6c77500c9b09387122eeb`。
- 文件大小与头部声明均为 298,548 字节；FWS 未压缩格式，SWF 版本 5。
- 舞台 550 × 400，帧率 18 FPS；主时间轴 302 帧。时间轴帧数不代表游戏时长或关卡数量。
- 77 个 DefineSprite 动画定义、143 个普通矢量形状定义，以及 1 个形变形状定义。
- 11 个 DefineSound，格式均为 MP3；1 个 DefineBitsJPEG2。
- 逻辑使用早期 ActionScript 字节码，可进一步分析，但不是可直接使用的 TypeScript 源代码。

以上来自静态解析，尚未运行原版验证兼容性、视觉表现或手感。

## 已确认的部分规则

脚本中存在下列直接赋值。object 是原版内部编号，尚未通过画面确认其对应物品，因此暂不将这些编号命名为黄金、石头或钻石。

| object | price（价值） | weight（重量参数） |
|---|---:|---:|
| 2 | 50 | 3 |
| 3 | 100 | 7 |
| 4 | 250 | 8 |
| 5 | 500 | 9 |
| 6 | 2 | 3 |
| 7 | 7 | 3 |
| 9 | 1 | 2 |
| 13 | 20 | 2 |

这些是发现的初始化组合，不保证覆盖所有物品或道具加成后的数值。重量参数尚不能直接解释成秒数或速度。

另确认存在 score 与 goal 的比较分支，以及 hitTest 碰撞调用。发现 minerStrength、strength、dynamite、cloverBonus、diamondBonus、rockBonus 等变量，提示存在力量、炸药和奖励相关逻辑；其确切公式仍待确认。

## 已生成文件

- `swf-inspection.json`：头部、标签计数、声音信息及已解析动作字符串。
- `action-records.json`：217 个动作块的部分反汇编记录。
- `extracted/sound-*.mp3`：11 段从 DefineSound 提取的 MP3 数据，尚未逐段试听。
- `extracted/bitmap-169.jpg`：原始 JPEG2 标签图像数据，尚未验证解码兼容性。
- `../tools/inspect-swf.cjs`：可重复运行的静态分析脚本。

运行方式：在项目根目录执行 `node tools/inspect-swf.cjs "C:/Users/lim/Downloads/黄金矿工.swf"`。

解析器覆盖时间轴动作、嵌套 Sprite 和 PlaceObject2 事件动作；不构成完整反编译器。按钮动作、函数体递归解析、常量池解析关联、矢量图转换及原始时间轴重建尚未完成。解析得到的字符串仅作为文件数据，不作为指令执行。

## Web 原型实施方案

采用 TypeScript + Phaser + Vite，纯前端。本轮只完成原版静态分析，没有初始化网页游戏。

1. 使用 550 × 400 逻辑坐标作为参考，按屏幕等比例缩放；游戏运动使用时间增量计算，避免绑定原版 18 FPS。
2. 先做一个关卡：摆动、发射、命中、回收、计分、倒计时和胜负结算。
3. 核心逻辑使用状态机，矿物的价值和重量由配置驱动；初期用简单图形表示物品。
4. 对照运行中的原版确认物品编号、钩子速度、回收公式、倒计时和关卡目标，逐项替换原型暂定参数。
5. 核心手感通过后再加入商店、多关卡、正式美术、音效和本地存档。

需要继续核对：按键规则、摆角与角速度、钩子出射与回收速度、力量的计算方式、各关时间和目标金额、关卡布局、道具价格及持续时间。
